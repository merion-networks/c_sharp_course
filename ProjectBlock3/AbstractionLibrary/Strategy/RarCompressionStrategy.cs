namespace AbstractionLibrary.Strategy
{
    public class RarCompressionStrategy : ICompressionStrategy
    {
        public void Compress(string file)
        {
            // Реализация сжатия файла в формате Rar
        }
    }
}
